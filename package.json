{
  "name": "tradex",
  "version": "1.0.0",
  "description": "TradeX - Full Stack Trading App",
  "private": true,
  "scripts": {
    "start": "concurrently --names \"BACKEND,FRONTEND,DASHBOARD\" --prefix-colors \"cyan,green,yellow\" \"npm run server\" \"npm run frontend\" \"npm run dashboard\"",
    "server": "npm start --prefix backend",
    "frontend": "npm start --prefix frontend",
    "dashboard": "npm start --prefix dashboard",
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix dashboard"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
